<div class="container">
  <div class="composition-index">
    <h1>Mes Compositions</h1>

    <ul>
      <% @compositions.each do |composition| %>
        <li><%= link_to composition.title, composition_path(composition) %></li>


      <% end %>
    </ul>

  </div>

</div>
